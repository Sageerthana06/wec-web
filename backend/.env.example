PORT=5000
MONGO_URI=mongodb://localhost:27017/business_website
JWT_SECRET=supersecretjwttokenkeyshouldbelongandsecure
JWT_EXPIRE=30d

# Cloudinary Config (Optional, falls back to local uploads if empty)
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# SMTP Config (Optional, falls back to console logging if empty)
SMTP_HOST=smtp.mailtrap.io
SMTP_PORT=2525
SMTP_USER=
SMTP_PASS=
ADMIN_EMAIL=admin@yourcompany.com
